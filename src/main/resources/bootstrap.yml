spring:
  cloud:
    kubernetes:
      config:
        name: default
        namespace: default
        sources:
         - name: binding-think-it-through
         - name: event-streams-connecton-info